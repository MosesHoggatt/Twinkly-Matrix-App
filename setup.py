from setuptools import setup, find_packages

setup(
    name="TwinklyWall",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        "pygame",
    ],
    author="Your Name",
    description="A dot matrix display project",
)